# Graph.py

Graph.py: Library of python objects.
# Testing done through import in any other file or '__main__' section in Graph.py.

.sif and .tab files: Small test files to be loaded through Graph.loadSIF() and Graph.loadTab() respectively.
.sif = cytoscape compatible format. Only contains on verteces and edges. Weighting to be added separately. (Graph.addWeightVector() *not done yet*)
.tab = simple table format. Contains weighting vector.

All extra info such as weights and traversal results appended as .attributes to graph/node/edge object.
